# Only needed if turbo-core is re-enabled (stateless CSRF via header, for Turbo's fetch-based submits)
# framework:
#     csrf_protection:
#         check_header: true

turbo:
    broadcast:
        enabled: false
